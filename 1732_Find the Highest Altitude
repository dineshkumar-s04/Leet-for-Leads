class Solution {
    public int largestAltitude(int[] gain) {
        int alti=0;
        int max=0;
        for(int i=0;i<gain.length;i++)
        {
            alti += gain[i];
            max=Math.max(alti,max);
        }
        return max;
    }
}
